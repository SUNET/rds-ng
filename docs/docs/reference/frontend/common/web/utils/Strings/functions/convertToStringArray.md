# Function: convertToStringArray()

> **convertToStringArray**(`value`, `delimiter`): `string`[]

Defined in: [src/common/web/utils/Strings.ts:7](https://github.com/Sciebo-RDS/rds-ng/blob/9b7c190cc4aaf42711ecc0fce854b1587c853586/src/common/web/utils/Strings.ts#L7)

Converts an arbitrary value or an array of such to a string array.

## Parameters

### value

`any`

The value to convert.

### delimiter

`string` = `" "`

The delimiter to use for splitting a non-array value.

## Returns

`string`[]
